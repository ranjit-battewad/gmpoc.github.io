// This function is called when the Meet API is ready
function onApiReady(event) {
    console.log('Ranjit #### Meet API is ready.');
  }
  
  // Add event listener for when the Meet API is ready
  document.addEventListener('onApiReady', onApiReady);
  